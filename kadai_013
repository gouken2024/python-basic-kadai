def calculate_total(price:int,tax:int)->float:
  
  # 与えられた引数priceに消費税を加算し、変数totalに代入する
  total = price*(1+tax/100)

  # 変数totalの値を出力する
  print(f"代金は税込み{total}円です。")
      
# 関数を呼び出し、引数として購入金額を渡す
calculate_total(1000,10)
